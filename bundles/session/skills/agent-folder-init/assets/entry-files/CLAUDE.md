# {{PROJECT_NAME}}

Claude-specific additions only. Read `AGENTS.md` first for shared project instructions
and `.agents/memory/` for durable context.

Do not duplicate shared rules here. Add content only when Claude Code needs an entry
surface that cannot be expressed portably in `AGENTS.md`.
