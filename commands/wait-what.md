# Wait What - Re-pitch the last message

The last message did not land. Re-pitch it in plain English using `CONTEXT.md`
vocabulary.

## Usage

```bash
/wait-what
```

## Workflow

Use the `wait-what` skill. Do not restart the parent skill. After the re-pitch,
continue the parent flow.
