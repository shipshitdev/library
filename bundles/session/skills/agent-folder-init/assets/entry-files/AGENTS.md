# {{PROJECT_NAME}}

Shared project instructions for coding agents.

## Context

Read `.agents/memory/` for durable project facts relevant to the current task. Keep
short-lived session notes in `.agents/sessions/` when this repository tracks them.

## Rules and standards

Put shared actionable rules in this file. Put durable supporting detail in
`.agents/memory/`. Keep runtime policy, model selection, and execution environment in
the harness configuration.

## Task tracking

Use the repository's configured issue tracker. Do not create a parallel local task
system unless the project explicitly requires one.
